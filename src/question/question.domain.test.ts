/* eslint-disable fp/no-unused-expression */
/* eslint-disable fp/no-nil */

describe('question.test', () => {
  test('fields are mapped correctly', () => {
    expect(0).toBe(0);
  });
});

export {};
